{
  "name": "dify-deck-slidev",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.js",
    "build": "pnpm -r --filter './packages/*' --filter '!theme-dify' build && node scripts/generate-index.js",
    "serve": "pnpm build && npx serve dist -l 3000",
    "create-deck": "node scripts/create-deck.js"
  },
  "devDependencies": {
    "@slidev/cli": "^52.11.0",
    "@slidev/theme-default": "latest",
    "concurrently": "^9.1.2"
  }
}
