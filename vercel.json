{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm generate && pnpm -r --filter './packages/*' --filter '!theme-dify' build && node scripts/generate-index.js",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/:deck/:path*",
      "destination": "/:deck/index.html"
    }
  ]
}
